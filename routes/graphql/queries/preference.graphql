extend type User {
    "The user preferences."
    preferences(only: [String!] categories: [String!]): [UserPreference!]! @field(resolver: "IronGate\\Integration\\GraphQL\\Resolvers\\UserPreferences")
}
