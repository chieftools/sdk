"A team in the system."
type Team implements Entity {
    id: ID! @rename(attribute: "slug") @autoDocument

    "The name of the team."
    name: String!

    "The timezone configured for the team."
    timezone: String!

    createdAt: DateTime! @rename(attribute: "created_at") @autoDocument
}
